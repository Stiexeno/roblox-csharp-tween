-- Mirrors stubs/Ease.cs (LoopType enum). Numeric values consumed by
-- Tween/Sequence loop handling: 0 = Restart, 1 = Yoyo, 2 = Incremental.
return {
    Restart = 0,
    Yoyo = 1,
    Incremental = 2,
}
